[package]
name = "vtxo-tree"
version = "0.1.0"
edition = "2021"

[dependencies]
bitcoin = { version = "0.32", features = ["serde", "rand-std"] }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
bitcoincore-rpc = "0.19"
serde_json = "1"
